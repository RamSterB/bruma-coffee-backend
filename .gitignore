node_modules
dist
coverage
*.tsbuildinfo
.env
.env.local
.env.*.local
.DS_Store

# .vscode (solo se commitea settings.json)
.vscode/*
!.vscode/settings.json

# .devcontainer/ se COMMITEA a propósito (entorno reproducible)

# caché de instalación dentro del repo (no commitear)
.pnpm-store
.pnpm-store/**