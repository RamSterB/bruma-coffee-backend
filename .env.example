# Puerto HTTP de la API
PORT=8000

# PostgreSQL (contenedor docker postgres16 en la red brumacoffeenet)
DB_HOST=postgres16
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=bruma_coffee

# En desarrollo sincroniza el esquema automáticamente (false en producción)
DB_SYNCHRONIZE=true